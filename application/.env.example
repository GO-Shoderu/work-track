# Placeholders only. Do not configure the managed project before SQL approval.
# Browser-safe values, intentionally read only by server modules.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# Local development. Eventual production origin: https://worktrack.go-sh.dev
APP_URL=http://127.0.0.1:3000
# No service-role key, database password, or AI key belongs in the app config.
